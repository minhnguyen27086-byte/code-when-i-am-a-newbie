//Tính sin
#include <iostream>
#include <iomanip>
using namespace std;
#include <cmath>
float  calculate(int n){

if(n == 0) return 0.0;
if(n == 1 ) return 1.0;
return sqrt(n + calculate(n-1));
}

int main() {
	int n;
	cin >> n;
    cout <<fixed<<setprecision(2)<< calculate(n);
    return 0;
}
